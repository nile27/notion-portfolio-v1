# 🚀 기술 스택 UI 개편 및 노션 프로젝트 모달 최적화 리팩토링 일지

오늘 진행한 **기술 스택(Skills) 섹션 인터랙티브 개편**과 **노션 프로젝트 상세 모달 복구 및 스타일 최적화** 작업 내역을 정리한 리팩토링 일지입니다.

---

## 🛠️ 변경 및 개선 사항 (Changes Made)

### 1. 기술 스택 (Skills) UI 개편 및 고도화
- **그룹 카드 구조화 (Subskills)**:
  - 단일 스킬 나열 방식에서 연관 기술군을 묶어 보여주는 **그룹 카드 구조**(`State Management`, `CSS & Styling`, `.NET & WPF` 등)로 개편했습니다.
  - **Framer Motion 애니메이션**: 그룹 카드에 마우스를 호버하거나 모바일에서 탭하면 세부 기술 목록(설명, 레벨, 아이콘 등)이 부드럽게 세로로 펼쳐지는 인터랙티브 애니메이션 효과를 구현했습니다.
  - **신규 스택 추가**: 생산성 도구 섹션에 `Claude & Claude Code` 및 `Antigravity` 에이전트 스택을 추가했습니다.
- **반응형 세로 그리드 구성**:
  - 카테고리별로 깔끔하게 정렬된 세로 컬럼 스택 레이아웃을 통해 해상도에 맞게 유동적으로 카드 배치 개수가 반응하도록 개선했습니다.
- **히어로 영역 간소화**:
  - 기존의 히어로 이미지를 정리하여 포인트를 텍스트와 기술 스택 영역에 집중할 수 있도록 단순화했습니다.

### 2. 노션 프로젝트 상세 모달 복원 및 공식 스타일 최적화
- **iframe 방식 제거 및 `react-notion-x` 복구**:
  - 보안 정책(`X-Frame-Options: SAMEORIGIN`)으로 인한 외부 임베딩 차단 문제를 자체 파싱 렌더링 방식으로 롤백하여 완전히 해결했습니다.
- **노션 디자인 감성 일체화**:
  - `fullPage={true}` 적용으로 노션 페이지 본래의 **커버 헤더 배너**, **대표 이모지/로고**, **공식 타이틀**이 완벽히 표현되도록 고도화했습니다.
  - 모달 상단의 불필요한 기본 네비게이션바(`disableHeader={true}`)를 감추고, 우측 상단에 유리 효과가 들어간 깔끔한 **플로팅 컨트롤러**(노션 열기, 닫기 버튼)를 배치해 커버 이미지가 모달 상단 끝까지 꽉 차게 확장되도록 했습니다.
- **세부 스타일 디테일 튜닝**:
  - **단순 표(Table) 가로폭 문제 해결**: 모달 안에서 찌그러지던 노션 단순 표(`.notion-simple-table`)에 `!w-full` 너비 오버라이드를 적용하여 글자 깨짐 및 좁은 가로 폭 문제를 시원하게 해결했습니다.
  - **페이지 로고 테두리선 및 프레임 입체화**: 대표 로고(`rounded-full`, `shadow-sm`)에 4px 흰색 테두리선을 적용해 네모/동그라미 형태에 상관없이 자연스러운 원형 뱃지로 크롭되도록 했습니다. (다크 모드 시에는 투명 테두리로 투과되도록 분기 처리)
- **다크 모드 스타일 원복**:
  - 다크 그레이 색상(`#2F3437`)의 전체 사이트 및 카드 적용 실험을 거쳐 최종적으로는 기존 디자인 아이덴티티인 다크 브라운 톤(`#15130f` 배경색, `#323129` 보더 및 모달 `#191919`)으로 통일 및 원복했습니다.

### 3. 개발 환경 설정 및 경고 해결
- **VS Code CSS 문법 경고 해결**:
  - `globals.css` 내 Tailwind v4 특수 지시어 `@apply`, `@theme` 등으로 인해 발생하는 문법 경고를 `.vscode/settings.json` 설정(`"css.lint.unknownAtRules": "ignore"`) 추가로 완전히 해결했습니다.

---

## 🧪 검증 결과 (Validation Results)

- **Next.js SSR 빌드 검증**: `npm run build`를 수행해 TypeScript 정적 검사 및 Next.js Production 빌드 정상 통과를 최종 확인했습니다.
- **로컬 검증**: 로컬 개발 서버(`npm run dev`) 환경에서 기술 스택 카드의 인터랙션 동작 및 프로젝트 모달의 레이아웃, 표 출력, 커버 이미지 배치가 디자인 가이드라인에 맞게 정상 노출되는 것을 확인했습니다.
